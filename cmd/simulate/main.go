package main

func main() {

	// server := sse.New("Test01")
	// ch1 := server.Subscribe("abcd-1234")
	// ch2 := server.Subscribe("efgh-5678")
	// ch3 := server.Subscribe("abcd-1234")

	// // *ch <- fmt.Sprintf("testing\n\n")
	// fmt.Println("ch1", *ch1)
	// fmt.Println("ch2", *ch2)
	// fmt.Println("ch3", *ch3)

	// server.Unsubscribe("abcd-1234", *ch1)
	// server.Unsubscribe("efgh-5678", *ch2)
	// server.Unsubscribe("ijkl-9012", *ch2)

	// list := make(map[string]string)
	// list["test0"] = "apple"
	// list["test2"] = "banana"
	// list["test4"] = "coconut"
	// list["test6"] = "dewberries"

	// for i := 0; i < 8; i++ {
	// 	id := fmt.Sprintf("test%v", i)
	// 	f, ok := list[id]
	// 	if !ok {
	// 		fmt.Printf("%s not found\n", id)
	// 	} else {
	// 		fmt.Printf("found %s: %v\n", id, f)
	// 	}
	// }

}
